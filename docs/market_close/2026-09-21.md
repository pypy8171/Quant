# 장 종료 리뷰 — 2026-09-21 (월)

> 계좌: 모의(is_paper=true). 원장 `Quant/build_win/logs/trades_20260921.csv` 288행, 세션 5회(08:20×3·08:31·08:35).

## 요약
- A등급 결함(체결 누락·이중 발주·원장 불일치·주문 불능) 없음 — `check_runtime_health.py` 21개 판정 전원 PASS.
- 장 시작 전(08:20) SEH 크래시 3연속 발생, 하지만 신규 결함이 아니라 D-023(2026-09-08, "증분 빌드를 믿지 않는다")의 재발 — quant-a4가 08:31 `--clean-first` 재빌드로 이미 해소, 08:35 세션부터 정상.
- 종료 시점 당일손익 +160,094원(총평가 98,176,643원). DEVSCALE 신규 진입은 3종목(453340·012750·001120)뿐이고 나머지 20종목은 전일 이월분 청산(seed-trail) — 09-21 청산선 전환 이후 첫 거래일이라 매수보다 정리가 압도적이었다.
- "같은 시장가 매도 진행 중" 중복 발주 생략 88건은 재시도(최대 3회)로 대부분 자체 해소됐고, 하루 끝까지 미해소로 남은 주문은 0건(유령주문 재부활 0, `checked via` 원장 대사).

## 0. 오늘의 숫자
| | |
|---|---|
| 접수 / 체결 / 거부 / 취소 | 34 / 106 / 126 / 9 |
| 전략 신호 (BUY/SELL) | 9 / 71 |
| 실현 gross (원장 실현손익 열 합, 이월분·비용 포함) | -144,276원 |
| 당일손익(잔고 대조, 세션 시작 기준) | +160,094원 (총평가 98,176,643원) |
| 최장 무주문 구간 | 13:41:23~15:15:03 (93.7분) |
| 재스캔 계산 / 반영 | 1406 / 0 (마지막 전략 등록 이후 계산 1402 — D-14 아님, 등록 시점 이후라 정상) |

## 1. 코드적인 문제

### 1-1. 08:20 SEH 0xC0000005 크래시 3연속 — D-023 재발, 신규 결함 아님
- 증거: `Quant/build_win/logs/quant_trader.log:281822,281827,281832` — 08:20:34/08:20:40/08:20:48, 동일 addr=00007FF72D4981DC.
- 원인: `_private/HANDOFF_quant-c1.md`(로컬 전용, git 미추적) — `Quant/build_win`의 낡은 오브젝트(`StrategyBase.cpp.obj`·`EngineConfigure.cpp.obj`)가 헤더 변경(`StrategyBase.h`/`Engine.h`) 뒤에도 재컴파일되지 않음(ninja 의존 추적 결함, cl 한국어 로케일 "참고: 포함 파일:" 출력을 ninja가 못 읽음). 이미 [DECISIONS.md](../DECISIONS.md) D-023으로 결정·채택된 문제의 재발.
- 조치: quant-a4가 08:31 `--clean-first` 전체 재빌드로 해소, 08:35 세션부터 무크래시 확인. 근본(ninja 의존 추적 자체)은 D-023에서 이미 "고치는 비용 > 오브젝트 삭제 비용"으로 보류 결정됐으므로 이번에도 추가 조치 없음 — **오늘 고칠 것 없음**.

## 2. 매매적인 문제

### 2-1. 동시 보유 종목 한도 초과(22 >= 20)로 신규 진입 2회 정지
- 증거: 게이트봉쇄 사유 "동시 보유 종목 한도 초과 (22 >= 20, 실보유 22 선점만 0) — 신규 종목 진입 정지" ×2.
- 원인: 전일 이월 보유가 22종목으로 오늘 슬롯(20) 상한을 이미 넘어선 상태 — 코드 결함이 아니라 09-21 DevScale 청산선 전환 직후 첫 거래일이라 이월분이 많았던 결과.
- 조치: 청산이 진행되며 종목 수가 줄어드는 구조라 자연 해소 — 오늘 조치 불필요. 이월분이 계속 20종목을 넘으면 청산 속도가 느리다는 신호이므로 내일도 이 게이트봉쇄 건수를 관찰한다.

## 3. 주문관련 문제

### 3-1. "같은 시장가 매도 진행 중" 중복 발주 생략 88건
- 증거: 거부 사유 상위 대부분(45+10+8+7+6+4+4+4+3+2회 등)이 이 사유 하나. `[OrderThread] 청산 SELL 거부 → 재시도 예약 (1~3/3)` 로 재시도됨.
- 판정: 재시도 로직이 설계대로 동작 — 같은 종목에 대한 시장가 매도가 이미 진행 중일 때 새 신호를 생략하고 최대 3회까지 재시도한다. 하루 끝까지 미체결·미취소로 남은 주문 0건(원장 대사, `check_runtime_health.py` "유령주문 재부활" PASS)이므로 **최종 실패 0건** — 재시도 횟수가 많아 보이지만 결과적으로 전부 해소됐다.
- 조치: 해당 없음. 다만 재시도 로그량 자체가 많다는 것은 청산 신호 발생 빈도에 비해 시장가 체결 속도가 못 따라간다는 뜻이라, 청산 물량이 더 늘어나면(이월분이 많은 날) 재시도 큐가 밀릴 여지는 있다 — 지금은 관찰만.

### 3-2. KIS API 거부(빈 ODNO) 15건, 청산차단 미해소 알림 8건
- 증거: `EGW00201`(초당 거래건수 초과) 7건, 기타 빈 ODNO 8건. "청산차단 미해소 — 취소할 예약매도 없음/취소 실패 (수동 확인 필요)" 8건(093370·001820·017900×4·108490·004020).
- 확인: 8건 전부 해당 종목이 같은 날 안에 다른 주문으로 결국 청산 완료됨(예: 017900 광전자는 15건 재시도 끝에 301주 전량 매도, 004020·108490은 15:15 장마감 청산으로 완료) — **원장에 남은 미청산 없음**.
- 조치: 해당 없음. "수동 확인 필요" 로그 문구는 그 순간 기준 경고일 뿐 실제로 사람이 개입해야 했던 사례는 오늘 없었다.

## 4. 추가 발견
- HTTP 초당 한도 재시도(WinHTTP HTTP N) 1,100여 건 — 접수 지연 RTT 중앙값 848ms로 범위 내(`check_runtime_health.py` "초당한도 압박"·"주문 접수 지연" PASS), 조치 불필요.
- WS 구독 상한 넘침 9회 — 기존에 알려진 "2700종목 중 473종목만 체결" 구독 한도 이슈와 같은 계열로 보이나 오늘 체결에는 영향 없음(REST 폴링 대체 정상 동작) — 사용자 결정 대기 항목 유지, 오늘은 손 안 댐.

## 5. 다음 거래일 전까지 할 것
- [ ] 없음 — 오늘은 A등급 결함이 없어 즉시 조치 항목이 없다. 이월 보유 종목 수(2-1)만 내일 아침 게이트봉쇄 로그로 관찰.
