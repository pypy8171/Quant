# 장 종료 리뷰 — 2026-09-22 (화)

> 계좌: 모의(is_paper=true), 설정 `Quant/config/config_dev_paper.json`. 원장 `Quant/build_win/logs/trades_20260922.csv` 138행, 세션 7회.
> 리눅스 트레이더(`scripts/auto_trade_day.sh`)로 하루를 돈 첫 거래일이다.

## 요약
- A등급 결함(체결 누락·이중 발주·원장 불일치·주문 불능)은 없다. 15:30:04 마지막 창 닫힘 → 15:32:03 `session_done_2026-09-22` 기록 → 15:32:08 종료까지 자기 종료가 정상이었다(수집 214,114 · 신호 8 · 주문 6).
- 실현 −137,636원은 전부 전일 이월분 청산 12건이고 사유가 모두 "청산:존 이탈"이다. 당일 양변 라운드트립은 023810 2주 −20원 하나뿐이다. 매매비용 약 2.9만원(수수료 3.3천 + 거래세 2.6만)을 빼면 비용 전 −11.0만원이다.
- 오늘의 가장 큰 문제는 **오후 149.9분 무주문**이다. 피드·국면·슬롯·노출 한도는 전부 정상이었고, 원인은 진입 필터 `entry_atr_max_pct=4.0` 하나다. 하루 133회 판정 중 109회가 차단됐고 그중 79건이 ATR 단독 초과다.
- 노출은 15.8%로 끝났다. 설계 목표(슬롯 20 × 종목당 270~400만원 = 총자산의 55~82%)의 1/4~1/5이다. 청산은 규칙대로 돌았고 유입이 막힌 결과다.
- 리눅스로 옮긴 첫날이라 REST 쪽이 눈에 띈다 — CURL 타임아웃 28건, 잔고조회 실패 21건(최대 연속 3회). 그 때문에 13:34:35~13:40:22 **5.8분** 동안 신규 매수가 보수 정지됐다.

## 0. 오늘의 숫자
| | |
|---|---|
| 접수 / 체결 / 거부 / 취소 / 대조 | 67 / 38 / 6 / 23 / 4 |
| 마지막 체결 / 마지막 접수 | 11:39:18 / 13:00:37 |
| 전략 신호 (BUY/SELL) | 24 / 37 |
| 진입 필터 통과 / 차단 | 24 / 109 (차단율 82%) |
| 실현 gross (전부 이월분 청산 12건) | −137,636원 |
| 최장 무주문 구간 | 13:00:37~15:30:30 (149.9분) |
| 재스캔 계산 / 마지막 전략 등록 이후 계산 | 1,456 / 787 |
| 종료 시점 노출 | 15.8% (한도 95%) |

## 1. 코드적인 문제

### 1-1. 마지막 매매 창이 닫힌 뒤에도 재스캔이 돌아 창 밖 매수를 냈다 — 오늘 고침
- 증거: 15:30:30 `REJECTED DEVSCALE_005257 "세션 창 밖 (15:30, 허용 09:00~15:30)"`. 15:30:30에 재스캔으로 새로 붙은 005257이 곧바로 매수를 냈고 게이트가 막았다. 하루 판정 FAIL 1건의 내용이 이것이다.
- 원인: [Engine.cpp `maybe_rescan_universe`](../../Quant/src/core/Engine.cpp#L265) — 마지막 창이 닫혀도 재스캔 주기가 계속 돌았다.
- 조치: **끝났다**. `session_end_.config().close_min`과 `kst::sec_of_day`로 판정해 마지막 창이 닫히면 재스캔을 멈추는 조기 반환을 넣었다(main `8eecc23`). 창이 0개면 판정하지 않으므로 리플레이·테스트 동작은 그대로다.

### 1-2. 리눅스 curl 경로에 연결 재사용도, 분리 타임아웃도 없다
- 증거: CURL 타임아웃 28건 · "수신 제한 시간 초과" 21건이 거의 1:1로 붙는다. 시간대는 09시 10건·10시 13건으로 앞장에 몰린다.
- 원인: [KisTransport.cpp:387](../../Quant/src/api/KisTransport.cpp#L387)·[:430](../../Quant/src/api/KisTransport.cpp#L430) — 리눅스는 요청마다 `curl_easy_init()`/`curl_easy_cleanup()`이라 호출 하나가 TCP+TLS 핸드셰이크를 다시 낸다. 윈도우 경로는 [KisTransport.cpp:130-207](../../Quant/src/api/KisTransport.cpp#L130-L207)에서 세션·연결을 `thread_local`로 상주시킨다. 타임아웃도 리눅스는 [:406](../../Quant/src/api/KisTransport.cpp#L406) `CURLOPT_TIMEOUT=10`(전체 작업) 하나뿐이고 `CONNECTTIMEOUT`이 없어 연결과 수신을 갈라 볼 수 없다. 윈도우는 [:194](../../Quant/src/api/KisTransport.cpp#L194)에서 넷으로 나눠 준다.
- 덧붙임: 수신 타임아웃은 재시도하지 않는다([:464-468](../../Quant/src/api/KisTransport.cpp#L464-L468)). 즉 21건은 각각 10초를 버린 단발 실패다.
- 조치: 리눅스 핸들을 `thread_local`로 상주시키고 `curl_easy_reset()`만 한다. `CURLOPT_CONNECTTIMEOUT_MS=3000`, `CURLOPT_TCP_KEEPALIVE=1`을 넣고 `CURLOPT_TIMEOUT`은 10초를 유지한다. 전송 실패면 핸들을 버려 다음 호출에서 다시 맺는 것도 윈도우와 같게 맞춘다. 마감 뒤 배포 — 오늘 고칠 것.
- 주의: `_private/PROJECT_FACTS.md` P-2("커넥션 풀링, 450표본에서 지연 이득 없음")와 겹쳐 보이지만 그 측정은 윈도우 WinHTTP였고 여기는 리눅스에 그 재사용이 아예 없다는 것이라 대상이 다르다. 커밋 메시지에 이 구분을 적는다.

### 1-3. 프리페치의 잔고 조회는 실패를 캐시하지 않아 3초마다 다시 부른다
- 증거: 초당 한도 재시도 37건과 잔고조회 실패 21건이 같은 날 함께 올랐다.
- 원인: [DeviationScaleStrategy.h:1294](../../Quant/include/strategy/DeviationScaleStrategy.h#L1294) — 총평가금 캐시를 `static_equity > 0.0`일 때만 쓴다. 조회가 실패해 0이 돌아오면 캐시가 안 되고, 프리페치 풀 주기(3초)마다 전 종목이 각자 다시 부른다. 실패가 부하를 키우고 부하가 실패를 키우는 고리다.
- 조치: 실패(0)일 때도 **60초 쿨다운**을 기록해 그 안에는 다시 부르지 않게 한다. 성공 캐시는 현행(하루 1회) 그대로. 마감 뒤 배포 — 오늘 고칠 것.

### 1-4. WSL에서 하루 판정기가 통째로 죽던 것 — 오늘 고침
- 증거: 09-22 하루 판정이 한 줄도 나오지 않았다.
- 원인: `scripts/check_runtime_health.py`의 `import_psycopg2`가 WSL에서 윈도우 venv(`.venv-win`)의 psycopg2를 붙였고, wheel 안 `os.add_dll_directory`가 `AttributeError`를 냈는데 `except ImportError`만 있어 판정기 전체가 죽었다.
- 조치: **끝났다**. 플랫폼별 venv만 후보로 두고 `except Exception`으로 넓혔다(main `8eecc23`). WSL Ubuntu-24.04에 `python3-psycopg2`를 설치해 DB를 보는 판정 6행이 다시 판정한다.

## 2. 매매적인 문제

### 2-1. 진입 필터 ATR 4%가 후보의 82%를 하루 단위로 잘랐다 — 오후 무주문의 원인
- 증거: 진입 필터 판정 133회 중 통과 24 · 차단 109. 차단 사유는 **ATR 단독 초과 79건**, 개장이격 단독 미달 0건, 둘 다 1건. 차단분 ATR14 중앙값 5.55%(기준 4.0%). 진입 신호 `정배열눌림진입`은 하루 24건뿐이고 시간대별로 09시 13 · 10시 8 · 11시 1 · 13시 1 · 14시 0 · 15시 1이다.
- 배제한 것: 봉 닫힘이 13시 1,309건·14시 1,415건으로 10시(1,327건)와 같아 피드는 정상이었다. `regime.json`은 `entry_halt:false`·RISK_ON·`entry_scale:1.0`이었다. 오후 유니버스는 13:30 매수가능 14종목·14:30 매수가능 18종목이었고 재스캔도 오후 552회 돌아 신규 등록 11건을 냈다. 슬롯은 20개 중 4~5개만 찼다.
- 원인: [DeviationScaleStrategy.h:1091-1127](../../Quant/include/strategy/DeviationScaleStrategy.h#L1091-L1127) `day_entry_allowed()` — `entry_filter_date_ != today` 가드 때문에 종목당 하루 한 번, 첫 평가 때 판정해 그날 내내 고정된다. 오후에 재스캔으로 새로 등록된 종목도 등록 즉시 한 번 판정되고 ATR>4%면 그날은 끝이다. 값은 [StrategyFactory.cpp:691](../../Quant/src/strategy/StrategyFactory.cpp#L691)에서 읽는다.
- 구조적으로 두 축이 반대로 당긴다 — 스캐너는 `sector_min_chg=1.0`·거래대금 랭킹·정배열 눌림으로 변동성이 큰 종목을 골라 오는데, 진입 필터는 ATR14 4% 이하만 통과시킨다.
- 조치안(**사용자 승인 필요 — 전략 파라미터**): `entry_atr_max_pct` 4.0 → **5.5**. 오늘 데이터로 되돌려 계산하면 통과 16 → 55(약 3.4배), 39종목이 풀린다. 6.0은 통과 90으로 필터를 사실상 끄는 것이라 넣지 않는다. 5.5는 차단분 ATR 중앙값 5.55를 경계로 잡아 "오늘 걸린 것의 절반"을 푸는 값이다.
  - 판정은 진입 건수가 아니라 진입당 기대값으로 한다. ATR을 푸는 만큼 `stop_loss_pct=6.5`가 더 자주 맞을 수 있다. 합격선: 5.5 슬리브의 진입당 평균 R이 4.0 슬리브 이상, 표본 40진입 이상. `id_prefix`를 갈라 같은 날 두 슬리브를 동시에 돌리면 하루에 대조된다.

### 2-2. 재호가 문턱 8틱이 넓어 2시간 30분 미체결로 남았다
- 증거: 13:00:37 접수한 `ORD-000006 DEVSCALE_000325 BUY LIMIT 71 @27,300`(진입사유 "이격=0.8% SMA20=27,635 현재가=27,850", 시장가 대비 −2.0% 지정가)이 15:30:29 진입 축이 닫힐 때까지 한 번도 재호가되지 않았다. 원장 `Quant/build_win/logs/trades_20260922.csv` 137~139행.
- 원인: `reprice_move_ticks=8` — 호가단위 50원 기준 약 400원(1.4%)이다. 3분봉 전략의 재호가 문턱으로는 넓다.
- 조치안(**사용자 승인 필요 — 전략 파라미터**): 8 → **3**(약 0.5%). `min_rebuild_sec=5`가 잦은 재호가를 막는 1차 방어선이라 폭은 좁혀도 된다. 판정 기준은 BUY 접수 대비 체결률 40% 이상(오늘은 접수 24에 취소 23), 하루 취소 60건 이하.

### 2-3. 노출 15.8%는 의도가 아니다 — 다만 청산선 변경 탓은 아니다
- 증거: 총평가 98,111,461원(15:31:57 잔고 대조)의 15.8% ≈ 1,550만원. 설계 목표는 `max_concurrent_positions=20` × 종목당 270~400만원 = 5,400~8,000만원(총자산의 55~82%)이다.
- 09-21 청산선 변경이 원인이 아니라는 근거 셋: ① `displace_enabled=false`는 슬롯이 찼을 때 쓰는 기능인데 20개 중 4~5개만 찼다 ② 손절 −2%·익절 1.2%는 config상 TRENDX 블록(`stop_loss_pct=2.5`)이고 오늘 돈 주 슬리브는 `stop_loss_pct=6.5`다 ③ 오늘 청산 12건은 전부 "청산:존 이탈"이지 손절·익절 트리거가 아니다.
- 판정: 노출은 결과이지 원인이 아니다. 2-1·2-2를 고치면 따라 올라간다. `max_gross_exposure_pct 0.95`·`max_concurrent_positions 20`은 오늘 제약이 아니었으므로 건드리지 않는다.

## 3. 주문관련 문제

### 3-1. 거부 6건 — 최종 실패 6건, 전부 창 끝에 몰렸다
- KIS 빈 ODNO 3건, 취소 거부 2건, 세션 창 밖 1건.
- 취소 거부 2건 중 하나는 15:30:30 `"KIS 취소 거부 [40580000] 모의투자 장종료 입니다"`로, 15:30:29에 진입 축이 닫혀 미체결 취소를 냈는데 모의 장종료 시각과 1초 차로 엇갈린 것이다. 창 밖 1건은 1-1에서 고쳤다.
- 조치: 청산·취소 시각을 창 끝보다 앞으로 당기는 문제는 [D-22](../DEFERRED_ISSUES.md)에 이미 열려 있다. 오늘 건을 증거로 붙였다.

### 3-2. 취소 23건이 접수 67건 대비 많다
- 체결 38 / 취소 23이다. 취소 대부분은 2-2의 재호가 문턱 때문에 오래 걸려 있다 축이 닫히며 거둬들인 것이다. 2-2를 고치면 같이 내려간다 — 별도 조치 없음.

### 3-3. 초당 한도 재시도 37건 — 대기가 공정하지 않다
- 증거: 느린 조회의 내역이 `경과=8864ms 버킷대기=8578ms`처럼 **대부분 버킷 대기**다. 장중 최대 조회는 `경과=9577ms 버킷대기=0ms`(10초 타임아웃에 걸린 것)이고, 장전 01:08 스캔은 `전체=120790ms(REST=67976ms 간격sleep=46500ms)`였다.
- 원인 둘: ① [UniverseScanner.cpp:1208-1211](../../Quant/src/universe/UniverseScanner.cpp#L1208-L1211)의 종목 간 150ms 고정 간격 = 초당 6.7건인데 모의 버킷은 `refill=2.0`([KisTransport.cpp:505](../../Quant/src/api/KisTransport.cpp#L505))이다. 위 46.5초가 150ms × 약 310종목이다. ② 비우선 호출은 `need=2.0`으로 버킷 만재를 요구하고([:510](../../Quant/src/api/KisTransport.cpp#L510)), 대기 루프가 FIFO가 아니라 0.2초마다 다시 경합한다([:540-545](../../Quant/src/api/KisTransport.cpp#L540-L545)) — 경합자가 많으면 한 호출이 오래 밀린다.
- 조치: ①만 오늘 고친다 — 모의면 종목 간 간격 **600ms**, 실계좌는 현행 150ms 유지. ②(대기 순번화)는 동작이 넓게 바뀌므로 전후 측정을 붙여 단독 커밋으로 미룬다.

## 4. 추가 발견

### 4-1. 기각된 TRENDX 슬리브가 라이브 설정에 살아 있다
- 확인: `Quant/config/config_dev_paper.json`에 `id_prefix: TRENDX`인 `DEVIATION_SCALE` 블록이 있고(`stop_loss_pct=2.5`), `regime_strategies`의 BULL·NEUTRAL 둘 다 `TRENDX_*`를 포함한다. 오늘 라벨은 RISK_ON이었다.
- 오늘 실제 영향은 없다 — 로그의 TRENDX 줄 21개는 전부 기동 시 등록 문구(세션 7회 × 3줄)이고 `universe_from_scan: 초기 0종목 등록`에서 더 나아가지 않았다. 신호·주문 0건이다.
- 다만 메모리 `project_live_strategy_status.md`는 "TRENDX는 D-101로 꺼짐, 다시 올리지 말 것"이다. **설정과 결정이 어긋나 있다** — 내려야 하는지는 사용자 판정 사안이라 §5에 올린다.

### 4-2. 잔고조회 실패로 신규 매수가 5.8분 정지됐다
- 증거: 13:34:35 `daily_pnl 갱신 끊김(streak≥2) — BUY NEW 보수 정지(B2)` → 13:40:22 `신규 진입 정지 해제(B2)`. 잔고조회 실패 21건 중 최대 연속은 13:37:04의 streak=3이었다.
- 실패 시 [LedgerReconciler.cpp:341-344](../../Quant/src/core/LedgerReconciler.cpp#L341-L344)는 WARN만 남기고 `resync_holdings`를 부르지 않는다. 원장을 망가뜨리지는 않지만 `refresh_sellable`·놓친 매도 되메움·`set_available_cash`·`set_equity`가 그 창 동안 함께 멈춘다. 빈 응답을 정본으로 믿는 경로는 [:153](../../Quant/src/core/LedgerReconciler.cpp#L153)의 가드로 막혀 있다.
- 판정: A등급 아니다. 정지 창이 5.8분으로 짧고(A 기준으로 잡은 누적 30분의 1/5) 그 시간대 주문 요구도 없었다. 1-2·1-3을 고치는 것으로 같이 줄어든다.

### 4-3. 청산선 메모리와 설정이 어긋난다
- 메모리 `project_devscale_exit_lines_2026-09-20.md`는 "평단 −2% 손절·익절 평단 1.2% 1층"인데, 라이브 설정의 주 슬리브는 `stop_loss_pct=6.5`이고 2.5는 TRENDX 블록에 있다. 어느 쪽이 정본인지 확인해 메모리나 설정 한쪽을 맞춰야 한다 — §5에 올린다.

### 4-4. 원장 CSV에 하네스 오염 행 14개가 남아 있다
- `Quant/build_win/logs/trades_20260922.csv` 09:42:25~26의 `buy_once_005930`·`buy_once_000660`·`BuyEachOnce`(ODNO 9000000001~3 @70,100). DB 쪽 행은 오전에 지웠고 CSV에는 남아 있다.
- 근본 조치는 부하 하네스에서 ZMQ 발행을 끄는 것이다. 오늘 손익·판정에는 영향이 없다(당일 양변 라운드트립 집계에 안 잡힌다).

### 4-5. 진입이 왜 안 났는지를 로그가 말해 주지 않는다
- 오후에 매수가능 종목이 14~18개 있었는데 각각 어떤 조건에서 탈락했는지 로그로 가를 수 없다. 오후 DEVSCALE DEBUG 3,215건이 전부 "봉 닫힘"이다. 이번 판정도 진입 필터 쪽 집계로 우회해 답을 냈다.
- 조치: `day_entry_allowed` 통과 뒤의 존·이격 판정을 초당 1회 표본으로 남긴다. 보류 항목으로 올렸다(D-23).

### 4-6. 하루 판정 WARN 4건
- WS 폴백 1회 · 주문 접수 지연 RTT 중앙값 1,773ms · 자원 표본 공백 580초 · 개장부터 적재 첫 틱 09:57. RTT는 1-2·3-3과 같은 뿌리다. 자원 표본 공백과 첫 틱 시각은 각각 quant-13·quant-c3 묶음에서 이미 손대고 있다.

## 5. 다음 거래일 전까지 할 것
- [ ] 리눅스 curl 연결 재사용 + `CONNECTTIMEOUT_MS=3000`·`TCP_KEEPALIVE` — [Quant/src/api/KisTransport.cpp:380-432](../../Quant/src/api/KisTransport.cpp#L380-L432) (1-2)
- [ ] 프리페치 잔고 조회 실패 60초 쿨다운 — [Quant/include/strategy/DeviationScaleStrategy.h:1294](../../Quant/include/strategy/DeviationScaleStrategy.h#L1294) (1-3)
- [ ] 스캐너 종목 간 간격을 모의일 때 600ms로 — [Quant/src/universe/UniverseScanner.cpp:1208-1211](../../Quant/src/universe/UniverseScanner.cpp#L1208-L1211) (3-3)
- [ ] `scripts/check_runtime_health.py`에 판정 행 추가: CURL 타임아웃 5건 이하 · 잔고조회 실패 3건 이하 · 초당 한도 재시도 10건 이하 · `BUY NEW 보수 정지(B2)` 누적 0분 (사람이 로그를 열어 보게 두지 않는다)
- [ ] **사용자 판정**: `entry_atr_max_pct` 4.0 → 5.5 (2-1), `reprice_move_ticks` 8 → 3 (2-2) — 둘 다 전략 파라미터라 승인 뒤에 넣는다
- [ ] **사용자 판정**: TRENDX 블록을 라이브 설정에서 내릴지 (4-1), 청산선 메모리와 설정 중 어느 쪽이 정본인지 (4-3)
- [ ] 부하 하네스의 ZMQ 발행 끄기 — 원장·DB 오염 재발 방지 (4-4)
