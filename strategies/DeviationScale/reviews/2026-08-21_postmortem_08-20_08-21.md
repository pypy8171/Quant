# DevScale 실증 사후검토 — 08-20 / 08-21 (에이전트 회의)

> 방식: 6축 로그 근거분석(log-reader) → 3자 패널(구현/전략/리스크) 회의 → 좌장 종합.
> 근거 로그: `Quant/build_win/logs/quant_trader.log` (08-20 9,025줄 · 08-21 4,379줄, 동일 파일 append). 소스 라인번호는 reviewer 패널이 실제 코드에서 확인.

## 한 줄 결론

두 크리티컬(**08-21 크래시로 EOD 미실행**, **08-20 매도가능=0 청산 실패**)은 "**안전망을 그것을 무력화한 스레드에 얹은**" 단일 구조결함이다. 실계좌 전환 전 **EOD 타이머 분리·크래시 워치독·손실한도 집행** 3건(P0)을 닫아야 한다.

## 0. 배경 사실 (확정)

| | 08-20 | 08-21 |
|---|---|---|
| 신규 매수 진입 | 34건(093190×16 등) | 0건(캐리오버 관리·probe만) |
| 체결(추정) | 7건(명시 체결로그 0 — 잔고 역산) | 0건 |
| 세션 당일손익 최저 | **−317,260원** (15:03:47, 손실한도 −300k **초과**) | −153,816원 |
| 세션 당일손익 마감/최종 | −203,600원 | +31,184원(12:57 크래시 시점) |
| EOD(15:15) 청산 | **실패** — 4종목 "매도가능=0" 백오프#1~#5 전량 무발주 | **미실행** — 12:57:07 프로세스 소멸 |
| 결과 | 5종목 overnight 이월 | 018260·161390 2박 이월 |

- 구조: `rest_price_feed` 모드, 체결은 30초 잔고 리컨사일로만 역추적(H0STCNI0 미배선), 모의계좌(50189928).
- 08-21 12:57:07 `[섹터] 업종 등락률` 로깅 도중 프로세스 소멸 — `[ERROR]`·정상종료·EOD 로그 전무. EOD 3시간 전 사망.

## 1. 6축 근거분석 요약

### ① 주문·체결 파이프라인
- **H0STCNI0 체결통보 미배선**(by-design): 명시 체결 이벤트 0건, 체결은 잔고 스냅샷 차이로만 사후추정. "체결 7건"은 역산치.
- **dev_buy 사다리 리프라이싱 루프**(by-design): 접수→수십초 후 취소→재접수. 093190은 10:29~14:00 20회+ 반복. 체결은 "취소 거부: 수량 없음"으로만 간접 확인.
- **리컨사일 잔고폴링 지연/누락**(bug): 30초 주기 + 12002 타임아웃 시 지수 백오프 최대 8사이클(수 분) 실명 → 청산가디언이 낡은 스냅샷 위에서 판단.
- 접수 RTT 편차 29ms~5,939ms(WinHTTP 동기 + HTTP 500 재시도).

### ② EOD 청산 실패 (CRITICAL)
- **`sellable_qty()` 하드 클램프**(bug): `ord_psbl_qty=0`이면 시장가 청산 자체를 보류 → 강제청산 에스컬레이션 경로 부재(강제청산 로그 0건).
- **12002 아웃티지가 "매도가능=0 잠김"으로 위장**(bug): 진짜 잠김(조회성공)과 조회실패(`get_balance` 예외→안전값0)가 동일 WARN. EOD 창 실유효 관측 백오프#1 **단 1회**.
- **read-before-cancel 레이스**(bug): 같은 하트비트에서 `cancel_all`(큐잉만)→`emit_liquidation`(동기 read) → 취소로 풀린 수량을 같은 틱에 못 봄.
- "잠김"의 정체: 모의계좌 **당일 매수분 당일 매도불가**(익일 반영). 익일 09:45 동일수량 정상 매도로 입증 → **paper에선 EOD net-flat 구조적 검증 불가**.

### ③ 08-21 트레이더 크래시 (CRITICAL)
- 12:57:07 `[섹터]` 데이터행 fwrite 도중 절단, 정상종료·ERROR 0건, 단일 세션(09:37→12:57 재시작 0).
- **근본원인 로그 단독 규명 불가**(unclear): 크래시 덤프·stderr 캡처 부재. 라이터가 종료 시 부분행을 남기므로 `[섹터]` 절단은 write 포인터 위치일 뿐 인과지점 아님.
- 확정 영향: EOD 미실행 → 5종목 overnight 재이월(08-20 실패분 복리).
- 프로세스 감시·자동재시작·하트비트 경보 부재.

### ④ 손실한도·리스크게이트
- 게이트 측정기준 = **리컨사일 당일손익(총평가−원가)**, 전일대비 아님(08-18 −504,481 발화로 확정).
- 08-20 −317,260(15:03:47) 한도 초과했으나 거부 로그 0건 — 위반 순간 제출 중 주문이 없었고(다음 신호 15:09, 그땐 −189,570으로 회복), 게이트가 **per-order 사전점검**이라 위반·주문 타이밍이 겹쳐야만 흔적.
- **CRITICAL 설계 갭**(bug): 손실한도에 **강제청산/킬스위치 액션 없음** — 신규 BUY만 막고 기보유는 방치. 한도를 계산만 하고 집행하지 않는다.
- 측정치가 REST 총평가 폴링 기반이라 노이즈 스윙 큼(6분새 +127,690 스윙) → 하드스톱 도입 시 EMA/최저 앵커·확인틱 필요.

### ⑤ 레짐·entry_halt 토글 (08-21)
- **히스테리시스 전무**(bug): 단일점 −3 ON/−2 OFF 경계에서 5회 플래핑(최소 간격 2분53초).
- 유발원: 환율(USD/KRW) vote가 −0.4% 경계에서 +1↔0 진동(0.08%p 미세변동). VIX(−1)·미국채10Y(0) vote 종일 고정 → 프레임을 상시 halt 경계에 압박.
- 사이드카 파일 자체는 신선(stale 경고 0). force_liquidate(−6) 미도달(최저 −3).
- 08-21 실피해 없음(신규 체결 0)이나 진입 활성일엔 접수 여부가 매크로 잡음에 좌우되는 비결정 리스크.

### ⑥ DevScale 진입 게이팅
- **3중 게이트는 병목 아님**: 08-21 DEVSCALE BUY 신호 **55건** 정상 발생, 접수 0건.
- **진짜 병목 = 동시보유 5종목 한도 포화**(bug): 08-20 EOD 청산 실패 캐리오버 5종목이 개장 전부터 전 슬롯 선점("5>=5" 거부 21건).
- 08-20(33건 접수) vs 08-21(0건) 차이의 결정변수 = **시작 슬롯 여유**(08-20 캐리오버 2종목 / 08-21 5종목), 3중 게이트·국면·유니버스 아님.
- 정배열 프리필터 통과율 5~7%(135종목 중 6~9)로 좁지만 08-21엔 binding 아님.

## 2. 패널 간 이견 (좌장 판정)

| 쟁점 | 이견 | 판정 |
|---|---|---|
| 손실한도 미로깅 성격 | 구현: 설계상 발동대상 부재(P2) / 전략·리스크: 집행 배선 부재(P0) | **집행 배선 P0(IMP-03)**, 초과 1회 WARN은 하위 병합 |
| H0STCNI0 미배선 | 5축 by-design / 구현: 파생버그 방치불가 / 전략: 사이징 무결성 붕괴(093190 7.58M > 캡 7.5M) | 정공법 배선 **P1(IMP-08)**, 그 전 즉효 **캡 클램프 P1(IMP-06)** 선행 |
| 게이팅이 수익을 죽이나 | 전략 교정: 08-21 halt는 정확히 옳았음(세션 +31,184 마감). 진짜 결함은 진입컷·사이징 | **수용** — 방어 게이트 유효, 우선순위는 진입컷·사이징 |
| paper 검증가능성 | 전략 단독 제기: 모의계좌 당일매도불가로 EOD net-flat forward-test 불가 | **청산 안정성 근거를 paper EOD에서 취하지 말 것(IMP-11)** |

## 3. 개선안 (IMP-01 ~ IMP-13)

우선순위 원칙: **손실통제(P0) → 청산 배관 정합(P1) → 관측성·격리(P1) → 전략 재배치(P1) → 저비용 방어(P2)**. LAB 합격 엣지 0개 동안 regime 상태머신 개조·파라미터 튜닝 금지(보류목록 준수).

| ID | 우선 | 노력 | 분류 | 제목 |
|---|---|---|---|---|
| **IMP-01** | **P0** | M | 리스크 | EOD 청산·손실컷을 시세이벤트→독립 벽시계 타이머(control_thread) 구동 분리 |
| **IMP-02** | **P0** | M | 관측성 | 크래시 관측성(SEH→C++예외 변환) + 외부 워치독 자동 재기동 |
| **IMP-03** | **P0** | M | 리스크 | 손실한도 −300k 집행 배선(신규 BUY 차단 + soft de-risk + 세션 진입예산) |
| IMP-04 | P1 | M | 리스크 | EOD 강제청산 에스컬레이션 상태머신(sellable tri-state + marketable-limit fallback) |
| IMP-05 | P1 | M | 버그수정 | 취소→매도 순차 상태머신(read-before-cancel 레이스 제거) |
| IMP-06 | P1 | M | 리스크 | 확정잔고 기반 종목 명목 캡 클램프(reserved_ 드리프트 봉쇄) |
| IMP-07 | P1 | S | 리스크 | manage_holdings eod_exit_hhmm 1600→1515 정렬 |
| IMP-08 | P1 | L | 버그수정 | H0STCNI0 체결통보 배선 / OrderRouter 상태 FILLED 전이 |
| IMP-09 | P1 | S | 버그수정 | Logger localtime 스레드안전 수정(4스레드 데이터레이스 제거) |
| IMP-10 | P1 | M | 관측성 | 관측 모니터(수급/섹터/매크로) 예외격리 + 리컨사일 신선도 격리 |
| IMP-11 | P1 | M | 전략 | DevScale INTRADAY 실계좌 셸빙 + paper EOD 검증불가 명시 + H7 우선 |
| IMP-12 | P2 | S | 전략 | entry_halt 히스테리시스(dwell + 확인카운터)로 −3 임계 휩쏘 억제 |
| IMP-13 | P2 | S | 리스크 | 사다리 회전 상한 가드(세션당 재호가·누적체결 명목 상한) |

### P0 상세

**IMP-01 — EOD/손실컷 벽시계 타이머 분리**
- 문제: EOD 청산이 `DeviationScaleStrategy.h:122` `on_trade_batch`(td 도착)로만 트리거 → td는 `data_thread_fn`(`Engine.cpp:547`) 공급. 크래시·12002 정체 시 EOD가 안 돎.
- 수정: `control_thread_fn`(`Engine.cpp:1174`)에 `kst_hhmm>=eod` 감시 가디언 신설 — 시세 무관하게 청산 시그널을 `order_queue_`에 직접 push. 전략 EOD 블록은 백업으로 유지.
- 검증: data_thread 강제정체(12002)에도 15:15에 청산 발주되는지 / 12:57 유사 kill 후 워치독 재기동 시 재부팅 가디언이 청산하는지.

**IMP-02 — 크래시 관측성 + 워치독**
- 문제: 12:57:07 무증상 소멸, `[ERROR]` 0건. `data_thread_fn`의 `catch(std::exception)`(`Engine.cpp:815`)이 접근위반(SEH)을 못 잡음(/EHsc).
- 수정: 스레드 시작부 `_set_se_translator` 또는 `__try/__except`로 폴트 주소·스택을 LOG_ERROR. 외부 워치독이 exe 소멸 시 자동 재기동 → 부트스트랩 재시드 후 EOD 가디언 청산.
- 검증: 의도적 널 역참조로 폴트 주소·스택이 로그에 남는지 / kill 후 N초 내 재기동·재시드.

**IMP-03 — 손실한도 집행 배선**
- 문제: `OrderGate.cpp:117-127` 손실한도가 신규 BUY에만 적용, 강제청산·kill 에스컬레이션 없음. −317,260 시점 봉쇄·경고 전무.
- 수정: `daily_pnl_ <= -300,000`이면 (1) 1회 WARN, (2) 기존 `set_entry_halt()` 재사용해 신규 BUY 전면차단(SELL/CANCEL 통과), (3) 청산 모드 플래그로 EOD 가디언(IMP-01) 경로 연결. 60%(−180k) 선제 soft halt. 세션 신규 BUY 명목 상한(자본 20%).
- 검증: 당일손익을 −180k/−300k로 유도 시 신규 BUY reject·청산 트리거 / 정상장 halt 미발동.

> 전체 문제·수정·검증 상세는 대시보드(Artifact) 및 워크플로 산출물 참조.

## 4. 후속 게이트

1. **실계좌 전환 게이트**: IMP-01/02/03(P0) 닫히기 전 실계좌 라이브 **금지**.
2. **청산 배관 정합(P1)**: IMP-04/05/06/07로 EOD net-flat 보장.
3. **관측성·격리(P1)**: IMP-08/09/10.
4. **전략 재배치(P1)**: IMP-11 — DevScale INTRADAY 실계좌 셸빙, 백테스트 가능한 H7(dip-buy EOD 절제실험(ablation), 2022 홀드아웃(격리검증) 사전등록)로 리서치 이동.
