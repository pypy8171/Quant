# 리셋 회의 결론 — 2026-09-19 (D-101)

> 1라운드 참가 16명(strategist·fundamental-quant·macro-quant·risk-behavior·data-sourcer·backtest-runner·bias-auditor·quant-analyst·
> planner·pm·intraday-analyst·reviewer·perf-optimizer·log-reader·market-brief·harness-engineer). 브리핑 `research/RESET_2026-09-19_BRIEF.md`,
> 헌장 `research/COUNCIL_CHARTER.md`. 숫자는 각자 오늘 실측한 값이고 경로가 근거다.

## 0. 한 줄 결론

지금 손익을 좌우하는 것은 알파가 아니라 **운영(재기동·이관·마감 청산)과 회전 비용**이고, 그 위에 쌓을 **검증된 엣지는 0개**다.
순서는 ① 손실 차단(config만, 월요일) → ② 백테스트 재건(비용 정의 통일·평단 결함·와이드 패널·walk-forward) → ③ 데이터 마트(수급·틱·
리서치는 오늘 밤부터, 재무·거시는 키 발급 뒤) → ④ 팩터 스터디(저변동성 바스켓부터) → ⑤ 리스크 규칙·상태 지속 코드화 → ⑥ 엔진 배선·대시보드.

## 1. 합의된 진단

### 1-1. 손익은 전략이 아니라 운영이 갉아먹었다 (log-reader, 원장 `Quant/build_win/logs/trades_20260908~18.csv`)

| 구분 | 09-14~18 건수 | 09-14~18 실현 | 09-08~18 실현 |
|---|---|---|---|
| 전략 신호 — 익절 | 376 | +4,730K | +5,384K |
| 전략 신호 — 손절(TRENDX 2.5%) | 336 | −3,964K | −4,163K |
| 전략 신호 — 존이탈·기타 | 106 | ±0 | +547K |
| **전략 소계** | 818 | **+766K** | **+1,768K** |
| 운영 — 청산 관리 이관(ITB) | 183 | −1,980K | −2,065K |
| 　└ 09-11 마감청산 7분 정지 → 09-14 갭 이월 19종목 | 73 | −1,019K | |
| 운영 — 마감 청산 | 211 | −496K | −478K |
| 운영 — 슬롯 교체(DISPLACE) | 662 | −483K | −374K |
| **운영 소계** | 1,064(매도의 56%) | **−2,962K** | **−2,924K** |
| 합계 | | −2,196K | −1,155K |

- 재기동 42회(09-14~18): **세션이 주도한 exe·config 교체 kill 29회(69%)**, 잔고 부트스트랩 실패 루프 9회(09-16 65분 엔진 부재 → 마감 청산 못 함),
  KIS 토큰 발급 한도 3회, SEH 1회. 엔진이 상태를 디스크에 안 남겨 재기동마다 포지션이 ITB로 이관(하루 31~141건), 물타기 분할 단계 재배치(09-18 005830 684만 집중).
- **09-18 25종목이 주말 이월**(19:50 청산이 전부 "세션 창 밖" 거부 2,188줄) — 월요일 갭이 다음 운영 손실이 된다.
- 비용 총 287만(2주) = 비용 전 이익의 1.9배. 전략 소계 +177만은 비용 위에 서지 못한다. 손절 336건 −396만은 거의 TRENDX 2.5% 하드스톱 — 3분봉 노이즈 대비 폭이 좁은지 백테스트로 먼저 본다.
- 원장으로 나눌 수 없는 것: 이관 시점까지의 미실현(전략 몫) vs 그 뒤 청산 타이밍(운영 몫). 필요한 열 `session_id·origin_strategy·handover_mark·signal_price`.

### 1-2. 엣지 0 (strategist·quant-analyst·bias-auditor)

- 채택 게이트 통과 0. 현 축(정배열∧이격도)은 원장에서 세 번 기각 — D-013(150조합 유의 승 0), D-064(TRENDX 게이트 t=−0.96), 스터디 15(t=−5.40).
- 라이브 매도 698건: 비용 전 +2,163원/건(t=1.23) → 비용 후 −1,949원/건. 비신호 매도 476건이 비용 후 −5,899원/건(t=−2.97). 필요 표본 ≈2,300왕복·80거래일 — 11일로는 어느 부호도 못 굳힌다.
- 리스크: 슬롯 25×캡 400만 > 총노출 95% = 풀투자. 전 종목 갭 −10%면 하루 −925만. `daily_loss_limit −100만`은 실현손익만·BUY만·**재기동마다 0 리셋**(`Quant/src/risk/OrderGate.cpp` `reset_daily`) → 실효 일한도 ≈ −1,000만.

### 1-3. 백테스트는 느리고 틀리다 (perf-optimizer·backtest-runner·reviewer)

- 비용 정의 네 곳(engine.py·metrics.py·devscale_replay.py 0.31% / OrderGate 0.21% / exit_ev.py 0.195%). SELL pnl이 마지막 BUY 가격 기준(`PYQuant/backtest/engine.py _execute`) → 승률 판정 전부 오염. 벤치 비용 0·상폐 전방채움.
- `engine.py run()` 251행이 매 거래일×매 종목 전수 스캔 = **O(일수²)**: 1,000종목×3년 26.1초, 3,000×7년 외삽 ≈7분/회, walk-forward 격자 50셀×4창 ≈7~8시간. `metrics.py::simulate`와 스터디 13 `sliding_window_view`가 같은 계산을 따로 함 — 엔진이 둘.
- 와이드 패널 실측: pivot 0.89초, `rolling(20)` 0.15초, 36셀 격자 14.0초(0.39초/셀), 캐시 읽기 0.11초. 분봉은 14,678파일(파일당 1.3ms) → 종목당 1파일이면 20초→1~2초.
- 2022 홀드아웃은 스터디 09·11·12·15가 재사용 → 소진. 사전등록이 산출물보다 늦어 증명 불가.

### 1-4. 데이터 (data-sourcer, 오늘 실호출)

- **네이버 구 HTML 전부 302 → `m.stock.naver.com/api/*` JSON이 열려 있다.** 메모리 `data_source_constraints`·`docs/DATA_SOURCES.md` 갱신 대상.
- `siseJson` 깊이 **1990~**(상폐 포함). 수급 이력 `/api/stock/{code}/trend?bizdate&pageSize=60` **2009-10~, 외인·기관·개인 순매수 주식수 + 외인보유율, PIT-A** — 3,081종목×2019~ ≈ 98.6k콜(4워커 1시간).
- 상폐 목록: FDR 0행(KRX 차단) → KIND `delcompany.do` POST 200. 캘린더: tradingeconomics HTML 200, investing 403.
- 틱: `capture_dir` 키 하나면 켜진다(`Quant/src/core/AppConfig.cpp:196`, `Engine.cpp:936`). 09-15 이후 나흘치를 이미 잃었다.
- TimescaleDB 오늘 5432 닫힘·컨테이너 0 → 저장 1순위는 parquet. 키는 `DATA_GO_KR_KEY`만(DART·ECOS·FRED 미발급, `OpenDartReader` 미설치).
- `bars_all_pit.parquet` 09-04 이후 갱신 0. `investor_flow_logger.py` 스케줄 미등록.

### 1-5. 이미 있는 배선 (새로 파지 않는다)

스캐너 z-점수 4축 → `OrderGate::set_entry_priority`; `regime.json entry_scale/entry_halt/force_liquidate`; `TickCapture`+`ReplaySource`+`PaperExecutor`;
전략별 귀속 필드(D-089); `displace_max_per_day` 구현됨(0=무제한); 수급 가중치 파싱됨(점수식에 항 없음); 체결강도 파싱됨(진입 스탬프만).

## 2. 결정 사항

### A. 월요일 09-22 라이브 — config만, 코드 0줄 (`Quant/config/config_dev_paper.json`)

| 키 | 지금 | 바꿈 | 근거 |
|---|---|---|---|
| `displace_max_per_day` | 0 | 5 | 교체 매도 662건 −48만/주 |
| `displace_min_hold_sec` | 900 | 3600 | 같음 |
| `displace_min_z_gap` | 0.5 | 1.0 | 같음 |
| `capture_dir` | 없음 | `PYQuant/data/ticks_raw` | 틱 이력 0, 비용 0. 100~250MB/일 추정, 첫날 실측 뒤 gz 순환 |
| ITB `market_close_exit_hhmm` | 2100(보호만) | 1515 | DEVSCALE 슬리브도 1505→1515로 통일(오너 09-19 23시). 이월분(ITB 청산 관리)만 마감 청산이 없어 09-18 25종목 이월·09-11 이월 −102만. 15:15 시장가(접속매매, 재발주 여유 5분) |
| TRENDX `max_universe` | 25 | **0** | 세 표본 음(D-064·15·라이브 손절 −396만). 재등록은 슬리브로만(§5) |

운영 규칙(코드 없이 오늘): **09:00~15:30 재빌드·kill 금지** — 핫픽스는 `entry_halt`·kill switch로, 재기동은 하루 2회 예산. `scripts/auto_trade_day.ps1`에
`QUANT_ALLOW_INTRADAY_RESTART` 없으면 거부하는 가드 + 감시견 backoff 5→60→300초. 이것은 CLAUDE.md "장중 운영 — 판단이 서면 실행한다"를 뒤집는다(§5 오너).

### B. 백테스트 재건 (W1~W2, 장 밖 파이썬)

1. `PYQuant/backtest/costs.py`(`CostSpec` 수수료 0.015%·매도세 0.20%(2026년 세율: 코스피 0.05%+농어촌특별세 0.15%, 코스닥 0.20%. 09-21까지 원장은 2024년 값 0.18%로 계산돼 있어 그 행은 `LEDGER_UNTIL_2026_09_21`로 되짚는다)·슬리피지 half-tick+충격) + `ledger.py`
   (평단 원장 = `OrderGate::on_fill_confirmed` 미러). 세 갈래·`scripts/trade_costs.py`·`exit_ev.py` 전부 여기서 import. 골든: 파이썬 vs C++ `test_order_gate` 10케이스 **오차 0원**.
2. `engine.py` SELL 평단·벤치 비용·상폐 −100% 수정, 251행 전수 스캔을 누적 인덱스로(O(n)). 이벤트 드리븐 검증용으로만 남긴다.
3. **와이드 패널이 정본**: `PYQuant/backtest/panel.py --build` → `PYQuant/data/cache/wide/{open,high,low,close,tv}.parquet`(Date×code float32, 종목 categorical 정수 id)
   + 파생 피처(sma·tv20·hi250·in_pool) 1회 생성(원본 mtime 키). 스터디 13 `add_features·simulate_code`를 여기로 옮기고 13·14가 import(기존 out/*.json 소수 4자리 일치).
   `factor.py`(월 리밸·5분위·IC·스프레드·회전율) · `walkforward.py`(2019-21→22 … 2023-25→26 5창, 격자, `ProcessPoolExecutor(8)`) ·
   `stats.py`(`13_trendx_gate/stats_util.py` 이관 + Deflated Sharpe + 블록 부트스트랩). 첫 스터디 `research/studies/18_factor_harness/`.
4. 분봉 `scripts/minute_compact.py` → `PYQuant/data/minute_by_ticker/<code>.parquet`(14,678→747파일), `devscale_replay.py` 148행 교체.
   분봉→TickCapture v1 변환기 `PYQuant/backtest/minute_to_capture.py` + `config_replay_paper.json` → C++ 전략 그대로 리플레이. `devscale_replay.py`는 패리티 골든으로 강등.

### C. 데이터 마트

규약: 원천·피처는 parquet `PYQuant/data/<source>/`(`ticker, effective_date, published_at, value, grade`, append-only). TSDB 테이블은 `PYQuant/db/schema.sql`에
추가만 하고 DB 살린 뒤 적재("그날 엔진에 준 점수" 발행 원장). PIT 조인은 `PYQuant/data/pit.py::as_of_join` 하나. 종목 id는 백테스트에서도 정수(원칙 6).

| 축 | 소스(엔드포인트)·등급 | 스크립트 | 저장 | 주기 |
|---|---|---|---|---|
| 일봉·상폐 | `api.finance.naver.com/siseJson.naver` 1990~ + KIND `delcompany.do` + data.go.kr getItemInfo diff (**A**) | `PYQuant/tools/naver_bars_backfill.py`(3,081×0.3초 ≈15분) | `bars_all_pit.parquet` v2, `pit_universe/delisted.csv` | 야간 증분 |
| 수급 | `m.stock.naver.com/api/stock/{c}/trend` 2009-10~ (**A**, 주식수) + KIS 30일 대조 | `PYQuant/tools/naver_flow_backfill.py`(4워커 0.15초, ≈1h) — **오늘 밤** | `investor_flow_pit.parquet`(date,ticker,frgn/inst/indiv_qty,frgn_hold_pct) | 야간 60행 |
| 재무·공시 | DART `fnlttMultiAcnt`(주요계정 14, 발효일=rcept_no 앞 8자리, **B**, 2015~, 2만/일) — 매출총이익·현금흐름은 `fnlttSinglAcntAll` 추가 필요 | `PYQuant/tools/dart_fin_history_fill.py --from 2015`(적재 끝, 1,920호출 1,002초) | `PYQuant/data/fin/dart_fs_raw.parquet`·`fin_point_in_time.parquet` | 야간 증분 |
| 거시 | ALFRED 판본(**A**) 14시리즈·ECOS(**B**) 11시리즈 2005~(적재 끝 85,493행)·관세청 1·10·20일 잠정치(**B**, 활용신청 대기) | `PYQuant/tools/macro_ingest.py --source all --since 2005-01-01` | `PYQuant/data/macro/<source>_<series>.parquet` | 06:30·17:00 |
| 틱·호가 | `capture_dir`(QTCAP v1, **A** forward) + `capture->dropped()` 로그 + gz 순환 | config 한 줄 | `PYQuant/data/ticks_raw/ticks_*.bin` | 장중 |
| 리서치 | `/api/research/company?pageSize=20` 최근 5페이지 + 상세 `researchContent`의 `stock.pstatic.net/*.pdf`(목록 ~4.5개월, **B**) → pdfplumber → 규칙+LLM 점수 | `scripts/eod_data_nightly.py` ③ | `PYQuant/data/research/YYYY-MM-DD.json` + pdf | 야간 + 장중 1h |
| 컨센서스 | `/api/stock/{c}/integration.consensusInfo`(**C** forward 스냅샷) | 같은 스크립트 ④ | `PYQuant/data/consensus/YYYY-MM-DD.parquet` | 야간 |
| 캘린더·이벤트 | tradingeconomics HTML·FRED releases·ECOS 공표일정·KIND `todaydisclosure.do` POST (**B**) | `scripts/event_feed.py` | `PYQuant/data/macro/calendar.json`, `disclosure/` | 야간·5분 |

- forward 4종(일봉 증분·수급·리서치·컨센서스·거시)은 **한 스크립트 `scripts/eod_data_nightly.py`·한 예약작업 `Quant Data Nightly`**(모의 21:30/실 22:00).
  배선: `scripts/market_close_timetable.ps1` `$plan` 행 추가 → `-Apply`, `.claude/hooks/cron-gate.ps1` 감시 목록, 허브는 자동. 순서 backfill(21:00)→적재(21:20)→팩터(21:40)→walk-forward 주 1회.
- 키 발급(사용자 10분): opendart.fss.or.kr·ecos.bok.or.kr·api.stlouisfed.org·data.go.kr 관세청 활용신청 → env `DART_API_KEY`·`ECOS_API_KEY`·`FRED_API_KEY`·`TRADEDATA_API_KEY`.
- 실패 시 마지막 성공 파일 유지, 5영업일 연속 성공 전엔 백테스트 입력으로 승격 금지.

### D. 신호 축 확장 순서

1. **저변동성 횡단면 바스켓**(20일 실현변동성 하위 20%, 월 리밸, 거래대금 ≥10억) — 지금 데이터로. 유일한 ✅(변동성 타게팅)과 같은 계열.
2. 단기 반전 + 52주 고가 근접을 바스켓 안 타이밍으로(제거실험 두 셀).
3. 적재 뒤: 저PBR×고ROE·GP/A·발생액·PEAD(DART), 외국인·기관 순매수 지속성(수급 — D-014 "수급 무효"는 이번 백필 뒤 같은 표본으로 재검정), 공시는 **배제 필터**(거래정지·유상증자·CB), 섹터 상대강도.
4. 거시는 종목 선택이 아니라 **사이징 오버레이**: 네 축(성장·물가·유동성·위험선호) z → 국면 배수(확장 1.0/과열 0.75/수축 0.5/회복 0.75) → `regime.json entry_scale`에 곱.
   발표 −15분~+30분 `event_halt`. 현 8지표 등락표는 위험선호 축 하위 구성원으로 흡수. 합격 엣지 0인 동안은 **표시만**, 게이트 적용은 판정 뒤.
5. 배선: 팩터는 **Python 한 벌** `PYQuant/features/compute_factors(as_of)` — 백테스트와 08:20 장전 잡이 같은 함수. 유니버스 파일 schema 2(`scores·composite·grade·factor_as_of`)
   → `UniverseScanner` `Features::external` 5번째 항(`score_w_external` 기본 0) → 기존 rank/z → `set_entry_priority`. 장중 갱신 값은 `FactorTable`(SymbolId 인덱스 atomic float 배열).
   죽은 축(12-1 단독·정배열 필터·외인 소진율 단독·지수 국면 오버레이)은 다시 돌리지 않는다.

### E. 리스크 규칙·상태 지속 코드화 (W4, worktree·엔진 정지)

- **상태 지속**: 체결마다 포지션·분할 단계·미체결·전략 서브원장을 `logs/state_<account>.json`에 원자적 기록, `try_bootstrap_ledger`에서 복원(`OrderGate::seed_strategy_position`).
  `[inv] Σ_strategy ≤ positions_[ticker]`. 잔고 5회 실패 → `trades_<date>.csv` 시드 + `set_entry_halt(true)` "청산 전용" 기동(65분 부재 재발 방지). 토큰 캐시 파일 재사용.
- **손실 분할 매수** `Quant/include/risk/LossLadder.h::step()`(순수 함수): 전일 총자산 대비 평가 **−1.0% → entry_halt**, **−2.0% → 전량 청산 → 보유 0 확인 뒤 kill + 익일 노출 50%**,
  주간 −4% → 3거래일 휴식. `force_liquidate_`는 `liquidate_mask_`(REGIME|LOSS)로. KST 날짜 래치. 재기동 시 `daily_pnl_`을 당일 CSV로 재시드.
- **마감 청산 시계 단일화**: `market_close_exit_hhmm`·게이트 세션 창·ITB 장 마감을 `session.close_hhmm` 하나에서 파생. 실패분은 감시견이 `manual_order`로.
- **회전 제동**: 종목당 하루 진입 1회(원장 영속), 교체 금지 조건(피해자가 장중 순매수 상위 30 또는 체결강도 ≥100), 손절 쿨다운 원장. 매도가능 0 드리프트(09-18 006360 736줄)는 잔고 재조회 뒤 보류 해제.
- **변경 규율**: 한도 완화는 주 단위·D-NNN 필수, 장중엔 조이기만. 파라미터 변경은 30왕복 표본 뒤·08:00~08:50 창에서만.
- 원장 CSV 4열 추가(`Quant/src/order/OrderRouter.cpp`): `session_id·origin_strategy·handover_mark·signal_price`(+`position_id·exit_reason·avg_cost·fee_krw·tax_krw·bid/ask·config_hash`).
  `scripts/trade_costs.py --attribution`이 §1-1 표를 자동 산출. `scripts/check_runtime_health.py` `DEFAULT_LOG`를 `Quant/build_win/logs/`로.
- 대시보드 신호등: 일손실 도달률(실현·평가), 총노출, 최대 단일 노출, 유효 종목 수, 회전, 비용/비용전, 교체 n/5, 재진입 비율, 재기동 n/2, 미연결 수, 이월 종목 수, 5일 DD.

### F. 규율 개정 (`research/GUARDRAILS.md`)

- ②-b deny-by-default → PIT 등급표(D-101, 표기 완료). ④ Calmar 1개 → 헌장 3층, 2022 잠금 → walk-forward 원장 `research/HOLDOUT_LEDGER.md`(최종 봉인 창 2026-01~, 스터디당 1회 개봉).
- `research/studies/<NN>/PREREG.md`(가설·유니버스·PIT·고정값·격자 전체·창·지표·임계·n_min·비용·`trials_prior`)를 산출물보다 먼저 커밋, `run_manifest.json`에 해시,
  `scripts/check_backtest.py`가 해시 시각 < 산출물 mtime 검사. 위반은 그것만으로 기각.
- 다중검정: 팩터 스캔 매수 후 보유 q=0.10 / 스윕 Deflated Sharpe / 겹치는 보유 블록 부트스트랩 95% CI. 시도 수 미기재 = CONDITIONAL.
- 라이브 게이트: `Quant/config/*.json` 전략 키 diff는 `strategies/<전략>/PARAM_LEDGER.md` 행 없이는 `commit_gate` [차단]. 근거 없는 행은 30일 안에 판정 없으면 내린다.
- 슬리브 양식 `strategies/<전략>/sleeves/SLEEVE_<id>.md`: 예산(종목 ≤50만·일 ≤−10만·총 ≤−100만 즉시 중지)·종료(100왕복 또는 60거래일)·판정(비용 후 왕복 기대값 >0 ∧ 일가중 t ≥1.5).
- 판정 단위는 매도 레그가 아니라 **왕복(FIFO, 개시 전략 귀속)**.

### G. 하네스 정합 (harness-engineer)

- `docs/sync_map.toml`에 규칙: `src=[COUNCIL_CHARTER, GUARDRAILS]`, `deps=[RESEARCH_COUNCIL#멤버, agents/bias-auditor.md, .claude/AGENTS.md]`. `.claude/AGENTS.md`에 새 에이전트 3행.
- `STRATEGIES.md` 죽은 경로 8곳(`.claude/commands/strategy-debate.md`·`AGENTS.md`·agents 5개) → `strategies/README.md`. `maintain.py --weekly`가 폴더 없는 `*.md`도 잡게.
- `/verify-backtest`: `python`→`py`, "2022 홀드아웃"→walk-forward 3층, 커밋 문구 삭제, PIT 등급 칸.
- 예약작업 둘: `claude_stock_study` rc=0x800710E0 = 배터리 조건(`-AllowStartIfOnBatteries`), 72h 한도→`PT1H`; `claude_dashboard_sync` rc=1은 UTF-16 로그·NativeCommandError 거짓 실패 → `-File` 래퍼 + `cmd /c >> log 2>&1`.
- `docs/DECISIONS.md` 새 항목 뒤 `py scripts/sync_ledgers.py` 실행(안 하면 Stop 훅 차단). `research/STRATEGY_LAB.md` §3에 "3-e 리셋 회의 가설" 절. `docs/facts.json`이 `Quant/config` 런타임 파일까지 담기 시작 — 스캔 필터 확인.
- 시니어 절은 요지 3줄, 헌장은 한 번만 읽히게(토큰).

### H. 대시보드·정보 (`scripts/dashboard_server.py`)

`/api/state`에 `research`(리포트 목록·PDF·점수)·`events`(다음 발표 카운트다운·서프라이즈·당일 공시, 유니버스 종목 플래그)·`factors`(점수·등급)·리스크 신호등.
리서치 점수는 진입 게이트가 아니라 `entry_scores` 병기 관찰 열로 시작(D-014 전례). 데이터는 §C JSON을 읽기만.

## 3. 판정 기준 (숫자, 사전등록)

| 대상 | 통과 |
|---|---|
| 횡단면 팩터 | 랭크 IC ≥0.03 ∧ ICIR(월) ≥0.5 ∧ 5분위 단조(Spearman ≥0.8) ∧ Q5−Q1 비용 후 >0, t ≥2 ∧ 5창 중 ≥3창 + ∧ 편도 회전 월 ≤30%(가치·퀄리티)/≤60%(PEAD) ∧ 비용 ≤ 스프레드 50% ∧ 용량(ADV20 1% ≥100억) |
| 시계열 전략 | 비용 후(0.23%) 초과수익 vs 같은 유니버스 매수 후 보유 월 t ≥2.0(사전등록 단일)/≥3.0(스윕 ≥20) ∧ 창 ≥3/5 + ∧ ±1 이웃 전부 >0·중심 Sharpe 70%↑ ∧ 비용 0.21/0.35/0.55% 3단에서 0.35%까지 >0 ∧ MDD ≤ 매수 후 보유+5%p ∧ DSR p<0.05 |
| 장중 전략(왕복) | 비용 후 ≥+10bp/왕복 ∧ 일-블록 부트스트랩 95% CI 하한 >0 ∧ 필요 N=(2.5·sd/μ)² 사전 계산·최소 40거래일 ∧ 비용/비용전 ≤0.5 ∧ 비신호 청산 ≤30%. 킬: 20일 뒤 CI 상한 <0 |
| 국면 배수 | 확장−수축 월수익 t ≥2.0 ∧ 27창 중 ≥16 ∧ ±1 강건 ∧ MDD ≥20% 감소 ∧ 전환 ≤6회/년. 미달이면 표시만 |
| 월요일 config(forward 2주) | 회전 ≤0.8회/일 · 비용 ≤17만/일 · 재기동 ≤2/일 · ITB 이관 0 · 이월 0 · 재진입 <15% · 교체 매도 <15% · 운영 매도 비중 56%→<20% · 신규 왕복 비용 후 ≥0. 미달 → 슬롯 25→15 |
| 데이터 | 일봉 v2 상폐 커버 ≥ KIND 90%·삼성전자 v1과 100% 일치 / 수급 결측 ≤1%·KIS 부호 일치 ≥99% / DART 커버 ≥90%·발효일 결측 0·정정 이중행 0 / 캡처 dropped 0·리플레이 체결 수 = 로그 / 스크랩 성공 ≥95% |
| 하네스 | 비용 골든 0원 · 2회 실행 sha256 동일 · 무작위 스코어 1,000회 IC 0±0.01, q<0.1 ≤5% · 전 종목 7년 단일 팩터 ≤10초 · 50셀 walk-forward ≤60초 · 캐시 로딩 ≤0.5초 · 상주 ≤400MB · 분봉 747종목 1년 ≤3초 · 스터디 13 소수 4자리 재현 |
| 엔진 | 미연결 69% → <5% · `strategy_realized_pnl` 빈 행 <5% · 엔진 부재 ≤5분/일 · 손절 보류 0 · `bench_latency_path` p50 ±1% |
| 하네스 정합 | `check_docs`·`sync_impact --diff`·`sync_ledgers --check` exit 0 · `STRATEGIES.md` 0건 · `AGENTS.md` 이름 수 = 20 · `claude_*` rc=0 3영업일 |

## 4. 우선순위·일정

| 순서 | 묶음 | 언제 | 완료 판정 |
|---|---|---|---|
| 1 | §A config(교체 제동 3키·`capture_dir`는 **적용됨**, 나머지는 오너 결정 뒤) + 장중 재기동 가드 | 09-22 월 08:30 전 | 재기동 뒤 `[Regime]`·시드 줄·캡처 파일, 15:22 이월 0 |
| 2 | §C 수급 백필·일봉 v2·`eod_data_nightly.py` 등록 | 오늘 밤~주말 | 데이터 게이트 |
| 3 | §B 비용 단일 소스·평단·와이드 패널·walk-forward | W1~W2 장 밖 | 골든 0원, 18번 스터디 첫 표, ≤60초 |
| 4 | §C DART·거시 백필(키 발급 뒤) | W2~W3 | 데이터 게이트 |
| 5 | §D 1·2(저변동성·타이밍) → 3(재무·수급) | W3~ | 3층 게이트, 음성이면 D-번호 |
| 6 | §E 상태 지속·손실 분할 매수·마감 시계·원장 4열 | W4 worktree | ctest + 미연결 <5% + 이관 0 |
| 7 | §D 5 유니버스 schema 2 + `FactorTable` + §H 대시보드 | Phase 3 | `test_universe_file_v2`, 08:20 잡 20영업일 무결 |
| 상시 | §G 하네스 정합 | 이번 주 | §3 하네스 정합 행 |
| 동결 | 큐 34·T-25·T-12·T-3·T-13-2 전송 분리·T-14 | 합격 전략 ≥1 뒤 | — |
| 실계좌 | T-17·T-18·T-8·prelive 3건 | 합격 전략 ≥1 ∧ forward 10거래일 비용 후 ≥0 뒤 | — |

## 5. 오너가 정할 것 — 결정(2026-09-19 22시)

| # | 항목 | 결정 |
|---|---|---|
| 1 | 장중 재기동 | **두 등급**. A등급(체결 누락·이중 발주·원장 불일치·주문 불능)은 즉시 수정→재기동. 그 외(리팩터·이름·문서·성능)는 장 마감 뒤 배포(모의 15:30, 실계좌 20:00). CLAUDE.md "장중 운영" 절 개정 + `scripts/auto_trade_day.ps1` 가드. 근본 수정은 재기동 뒤 보유분을 ITB로 넘기지 않고 원래 전략에 되돌리는 것(전략별 손익 귀속 수정과 같이). |
| 2 | TRENDX | 귀속 수정 전 `max_universe 0`, 수정 뒤 사전등록 슬리브 5종목·100왕복. 오너: "없애는 게 낫다면 그래도 됨". |
| 3 | ITB 마감 청산(`market_close_exit_hhmm`) | 2100→**1515**(처음 1525로 적었다가 오너 지적으로 수정: 15:20부터 마감 동시호가라 시장가가 15:30에야 체결되고 그 사이 백오프 재발주가 쌓인다. 15:15는 접속매매라 즉시 체결·재발주 여유 5분). ITB 청산은 코드상 시장가(지정가→시장가 아님). 이월 0. 09-18 25종목 이월은 21시 청산 주문이 "세션 창 밖" 거부된 것(2,188줄). ITB 관리 승계분과 DEVSCALE 슬리브 보유분 모두 15:15로 통일(모의). 실계좌는 애프터마켓(16:00~20:00)이 있어 D-097대로 **19:50**(`Quant/config/config.json` 반영). |
| 4 | 하드스탑 | 하드스탑 원칙은 동의. **계좌 손익 단계 한도(−1/−2/−4%) 수치는 미확정** — 고정 수치는 반등 시 기대수익을 전부 내놓는다는 반론. 현재 비중 조절(지표 스코어) 방식과 결합하는 안을 2라운드에서 논의. 종목 손절 폭은 18번 스터디 뒤. |
| 5 | 슬롯 | **20** + 교체 제동(같은 날 재진입 금지·교체 매도 하루 5건). 2주 forward 비용 후 ≥0 미달 시 15. |
| 6 | 실계좌 문턱 | 합격 전략 ≥1 + forward 10거래일 비용 후 ≥0. 지금은 모의라 바꾸는 것 없음, 문턱만 기록. |
| 7 | 성능(큐 34) | **동결 철회**. 워크트리에서 진행, 배포는 1번 규칙대로 장 마감 뒤, hot path는 전후 수치 필수(원칙 7). |
| 8 | 키 발급 | 오너가 직접 발급 뒤 알림(DART·FRED·ECOS·관세청). 커밋은 c05b613. |

## 6. 미해결·다음 라운드

- 비용 기준점: 백테스트가 원장 0.23%로 내려오되 슬리피지는 캡처 뒤 실측 — 그때까지 3단 감도.
- TRENDX `max_universe 0`이면 기존 보유의 청산 경로(ITB 래퍼·마감 청산)가 남는지 재기동 전 확인.
- `docs/DATA_SOURCES.md`·메모리 `data_source_constraints` 갱신(네이버 구 HTML 끊김, siseJson 1990~, trend API).
- 2라운드(pm·interviewer·bias-auditor 반박)는 4번(손실 단계 한도 vs 지표 스코어 비중 조절)을 첫 안건으로 연다.
- **2라운드 결과(09-20 새벽)**: 데이터 4소스 적재·통계 표준·스터디 19(미달)·20(기각)·D-103 — 종합은 `research/RESET_2026-09-19_R2/README.md`.
